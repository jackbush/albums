---
interface Props {
  text: string;
  attribution?: string;
  attributionLink?: string;
}

const { text, attribution, attributionLink } = Astro.props;
---

<blockquote class="quote">
  <p class="quote__text">{text}</p>
  {
    attribution && (
      <cite class="meta quote__cite">
        {attributionLink ? <a href={attributionLink}>{attribution}</a> : attribution}
      </cite>
    )
  }
</blockquote>

<style>
  .quote {
    padding: 1.5rem 0;
    /* Pulled in from the plate edge so it reads as an aside, not another plate. */
    margin-inline: auto;
    max-width: 46ch;
    text-align: center;
  }

  .quote__text {
    font-family: var(--font-heading);
    font-size: clamp(1.5rem, 1.1rem + 1.9vw, 2.25rem);
    font-weight: 400;
    line-height: 1.28;
    letter-spacing: -0.015em;
    color: var(--text-title);
    text-wrap: pretty;
  }

  .quote__cite {
    display: block;
    margin-top: 1.25rem;
    font-style: normal;
  }

  .quote__cite a {
    color: var(--text-link);
  }
</style>
